# US-020 — Configure the days and hours a court is available

## Status

Done

## Objective

As an administrator, I want to configure the days and hours a court is available.

## Functional description

This user story enables the administrator to configure the days and hours a court is available. It belongs to EPIC-04 — Court Administration.

## Acceptance criteria

- [x] Configure the days and hours a court is available is implemented and available to the administrator.
- [x] A court must have at least one active sport before it can accept reservations.
- [x] Business hours can be configured per complex.

## Business rules

- CRUD for `Court`.
- Association of one or more `Sport` to a court.
- `CourtAvailabilityRule` per day of week.
- `BusinessHours` per complex.
- Activation/deactivation of courts.
- Preparation for court-level time blocks.

## Validations

- Validate required fields and data types server-side.
- Return clear, structured error messages for invalid input.

## Error cases

- Invalid or missing input data.
- User is not authenticated or authorized.
- The referenced resource does not exist.

## Security considerations

- Authenticate and authorize every mutating request.
- Do not expose internal implementation details in error responses.
- Avoid logging sensitive data such as passwords, tokens, or PII.

## Technical notes

- `Court`: `Id`, `SportsComplexId`, `Name`, `Description`, `SurfaceType`, `Indoor`, `Status`, `CreatedAt`, `UpdatedAt`.
- `CourtSport` many-to-many join table.
- `CourtAvailabilityRule`: `CourtId`, `DayOfWeek`, `StartTime`, `EndTime`, `SlotDurationMinutes`, `IsActive`.
- Validate `StartTime < EndTime` and that slots fit within the range.

## Tests required

- **Unit**: domain logic and validation rules.
- **Integration**: API endpoint behavior and persistence.

## Out of scope

- Features explicitly listed in later epics or future sprints.
- Advanced analytics, reporting, or notifications not required for the MVP.

## Dependencies

- EPIC-03 — Sports Complex Administration.

## Definition of Done

- All acceptance criteria are implemented and verifiable.
- Relevant unit, integration, and E2E tests pass.
- Code review is approved.
- No secrets or sensitive data are committed.
- Documentation is updated if the change affects setup or API contracts.

## Project affected

Backend API.

## Suggested branch

`feature/US-020-configure-the-days-and-hours-a-court-is-available`

## Instructions for Devin

Implement the functionality that satisfies this user story for the administrator.
