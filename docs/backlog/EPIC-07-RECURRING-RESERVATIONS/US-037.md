# US-037 — Book a fixed weekly slot for a period I define

## Status

In Progress

## Objective

As a user, I want to book a fixed weekly slot for a period I define.

## Functional description

This user story enables the user to book a fixed weekly slot for a period I define. It belongs to EPIC-07 — Recurring Reservations.

## Acceptance criteria

- [ ] Book a fixed weekly slot for a period I define is implemented and available to the user.
- [ ] The reservation is linked to a court, user, complex, and time range.
- [ ] Overlapping active reservations on the same court are prevented.
- [ ] Reservation is confirmed immediately for the MVP.
- [ ] The recurrence period has an end date or a maximum number of weeks.

## Business rules

- Create a weekly recurring reservation rule.
- Validate availability across the recurrence period.
- Generate individual `Reservation` occurrences.
- Cancel a single occurrence.
- Cancel the entire series.
- Modify future occurrences without affecting history.
- Detect conflicts during generation.
- Two active reservations cannot overlap on the same court.

## Validations

- Validate required fields and data types server-side.
- Verify the court exists and is active.
- Verify the slot is not already reserved.
- Apply the policy configured for the reservation's complex, with global `CancellationPolicy` values as fallback, for cancel operations.
- Return clear, structured error messages for invalid input.

## Error cases

- Invalid or missing input data.
- User is not authenticated or authorized.
- The referenced resource does not exist.
- The requested slot is no longer available.
- The court is inactive.

## Security considerations

- Authenticate and authorize every mutating request.
- Do not expose internal implementation details in error responses.
- Avoid logging sensitive data such as passwords, tokens, or PII.

## Technical notes

- `RecurringReservation` entity: `Id`, `SportsComplexId`, `CourtId`, `UserId`, `DayOfWeek`, `StartTime`, `DurationMinutes`, `StartDate`, `EndDate`, `Status`, `CreatedAt`.
- Each occurrence creates a `Reservation` with `Source = Recurring` and `RecurringReservationId` populated.
- Generation must be transactional and conflict-aware.
- Consider a background or scheduled task if generation becomes expensive; for the MVP, synchronous generation is acceptable for reasonable ranges.

## Tests required

- **Unit**: domain logic and validation rules.
- **Integration**: API endpoint behavior and persistence.
- **Integration**: concurrency and conflict scenarios.

## Out of scope

- Features explicitly listed in later epics or future sprints.
- Advanced analytics, reporting, or notifications not required for the MVP.

## Dependencies

- EPIC-06 — Reservations.

## Definition of Done

- All acceptance criteria are implemented and verifiable.
- Relevant unit, integration, and E2E tests pass.
- Code review is approved.
- No secrets or sensitive data are committed.
- Documentation is updated if the change affects setup or API contracts.

## Project affected

Backend API.

## Suggested branch

`feature/US-037-book-a-fixed-weekly-slot-for-a-period-I-define`

## Instructions for Devin

Implement the functionality that satisfies this user story for the user.
