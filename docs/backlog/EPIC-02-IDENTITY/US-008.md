# US-008 — Log in with Google

## Status

In Progress

## Objective

As a user, I want to log in with Google so I can access the platform securely.

## Functional description

This user story enables the user to log in with Google so that I can access the platform securely. It belongs to EPIC-02 — Identity and Access.

## Acceptance criteria

- [ ] Log in with Google is implemented and available to the user.
- [ ] Google OpenID Connect authentication flow completes successfully.
- [ ] A valid JWT is issued after successful authentication.
- [ ] The JWT contains user id, email, name, and roles.
- [ ] Users can log in via Google and receive a valid JWT.

## Business rules

- Google OpenID Connect integration.
- JWT issuance and validation.
- User profile completion with mandatory phone number.
- Roles and authorization policies.
- Protected routes and endpoints.
- Preparation for future phone verification.

## Validations

- Validate required fields and data types server-side.
- Validate the Google token and map claims to local user data.
- Return clear, structured error messages for invalid input.

## Error cases

- Invalid or missing input data.
- User is not authenticated or authorized.
- The referenced resource does not exist.
- Google authentication fails or is cancelled.
- User does not exist locally and registration is required.

## Security considerations

- Authenticate and authorize every mutating request.
- Do not expose internal implementation details in error responses.
- Avoid logging sensitive data such as passwords, tokens, or PII.
- Store tokens securely; use `HttpOnly` cookies for refresh tokens.
- Validate JWT signature, issuer, and audience.

## Technical notes

- Use ASP.NET Core `AddJwtBearer` and Google authentication.
- Store `GoogleSubjectId`, `Email`, `FullName`, `PhoneNumber`, and `PhoneVerified`.
- Custom `IAuthorizationRequirement` for complex-scoped access.
- Frontend stores access token in memory and refresh token in `HttpOnly` cookie if implemented.

## Tests required

- **Unit**: domain logic and validation rules.
- **Integration**: API endpoint behavior and persistence.
- **E2E**: user flow through the frontend.

## Out of scope

- Features explicitly listed in later epics or future sprints.
- Advanced analytics, reporting, or notifications not required for the MVP.

## Dependencies

- EPIC-01 — Technical Foundation.

## Definition of Done

- All acceptance criteria are implemented and verifiable.
- Relevant unit, integration, and E2E tests pass.
- Code review is approved.
- No secrets or sensitive data are committed.
- Documentation is updated if the change affects setup or API contracts.

## Project affected

Backend API and Frontend application.

## Suggested branch

`feature/US-008-log-in-with-Google`

## Instructions for Devin

Implement the functionality that satisfies this user story for the user.
