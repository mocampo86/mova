# US-058 — Rate limiting on login and reservation endpoints

## Status

Ready

## Objective

As a security owner, I want rate limiting on login and reservation endpoints.

## Functional description

This user story covers rate limiting on login and reservation endpoints for the security owner. It belongs to EPIC-10 — Audit and Operations.

## Acceptance criteria

- [ ] Rate limiting on login and reservation endpoints is implemented and available to the security owner.
- [ ] Google OpenID Connect authentication flow completes successfully.
- [ ] A valid JWT is issued after successful authentication.
- [ ] The JWT contains user id, email, name, and roles.
- [ ] `/health` and `/health/ready` endpoints exist and are used by the deployment.
- [ ] Sensitive endpoints (login, reservation creation) have rate limiting configured.

## Business rules

- Audit log for administrative actions.
- Error monitoring and logging.
- Health checks and metrics.
- Basic operational documentation.
- Backup and recovery documentation.
- Rate limiting preparation on sensitive endpoints.

## Validations

- Validate required fields and data types server-side.
- Validate the Google token and map claims to local user data.
- Return clear, structured error messages for invalid input.

## Error cases

- Invalid or missing input data.
- User is not authenticated or authorized.
- The referenced resource does not exist.
- Google authentication fails or is cancelled.
- User does not exist locally and registration is required.

## Security considerations

- Authenticate and authorize every mutating request.
- Do not expose internal implementation details in error responses.
- Avoid logging sensitive data such as passwords, tokens, or PII.
- Store tokens securely; use `HttpOnly` cookies for refresh tokens.
- Validate JWT signature, issuer, and audience.

## Technical notes

- `AuditLog` entity: `Id`, `UserId`, `SportsComplexId`, `Action`, `EntityType`, `EntityId`, `CreatedAt`, `Metadata`.
- Use Serilog with correlation IDs propagated via `HttpContext`/`TraceIdentifier`.
- Application Insights for Azure; equivalent open-source alternatives for local/non-Azure environments.
- Rate limiting can be implemented with ASP.NET Core `RateLimiting` middleware.
- Keep audit logs immutable; do not update or delete audit records.

## Tests required

- **Unit**: domain logic and validation rules.
- **Integration**: API endpoint behavior and persistence.
- **E2E**: user flow through the frontend.

## Out of scope

- Features explicitly listed in later epics or future sprints.
- Advanced analytics, reporting, or notifications not required for the MVP.

## Dependencies

- EPIC-01 — Technical Foundation.
- EPIC-02 — Identity and Access.
- EPIC-06 — Reservations (for audit targets).

## Definition of Done

- All acceptance criteria are implemented and verifiable.
- Relevant unit, integration, and E2E tests pass.
- Code review is approved.
- No secrets or sensitive data are committed.
- Documentation is updated if the change affects setup or API contracts.

## Project affected

Backend API and Frontend application.

## Suggested branch

`feature/US-058-rate-limiting-on-login-and-reservation-endpoints`

## Instructions for Devin

Implement the functionality that satisfies this user story for the security owner.
