# US-023 — See a landing page with the platform value proposition

## Objective

As a visitor, I want to see a landing page with the platform value proposition.

## Functional description

This user story enables the visitor to see a landing page with the platform value proposition. It belongs to EPIC-05 — Public Availability and Discovery.

## Acceptance criteria

- [ ] See a landing page with the platform value proposition is implemented and available to the visitor.
- [ ] Results are filtered and paginated when applicable.
- [ ] Only active and public resources are returned to unauthorized callers.
- [ ] Response times remain acceptable for mobile users.
- [ ] Public endpoints do not expose internal/admin data.
- [ ] Only active complexes and courts are listed.
- [ ] Availability query considers `CourtAvailabilityRule`, `BusinessHours`, existing reservations, and `CourtBlock`.
- [ ] Slots are returned with local time conversion handled by the frontend.
- [ ] Response time for availability query is acceptable for mobile users.

## Business rules

- Public landing page.
- List and detail views for complexes.
- List courts of a complex.
- Filter courts by sport.
- Availability query by date and court.
- Display upcoming available slots.

## Validations

- Validate required fields and data types server-side.
- Validate query parameters and pagination inputs.
- Return clear, structured error messages for invalid input.

## Error cases

- Invalid or missing input data.
- User is not authenticated or authorized.
- The referenced resource does not exist.
- No results match the filter criteria.

## Security considerations

- Authenticate and authorize every mutating request.
- Do not expose internal implementation details in error responses.
- Avoid logging sensitive data such as passwords, tokens, or PII.
- Ensure users can only view data they are allowed to access (multi-tenancy).

## Technical notes

- Availability algorithm:
  1. Take the court's active `CourtAvailabilityRule` for the requested day of week.
  2. Split the range into slots of `SlotDurationMinutes`.
  3. Remove slots that overlap with active reservations or `CourtBlock` records.
  4. Remove slots outside the complex `BusinessHours` for that day.
- API: `GET /api/v1/complexes/{complexId}/availability?courtId=&date=`.
- All times stored in UTC; frontend converts to local time.

## Tests required

- **Unit**: domain logic and validation rules.
- **Integration**: API endpoint behavior and persistence.
- **E2E**: user flow through the frontend.

## Out of scope

- Features explicitly listed in later epics or future sprints.
- Advanced analytics, reporting, or notifications not required for the MVP.

## Dependencies

- EPIC-03 — Sports Complex Administration.
- EPIC-04 — Court Administration.

## Definition of Done

- All acceptance criteria are implemented and verifiable.
- Relevant unit, integration, and E2E tests pass.
- Code review is approved.
- No secrets or sensitive data are committed.
- Documentation is updated if the change affects setup or API contracts.

## Project affected

Backend API and Frontend application.

## Suggested branch

`feature/US-023-see-a-landing-page-with-the-platform-value-proposition`

## Instructions for Devin

Implement the functionality that satisfies this user story for the visitor.
